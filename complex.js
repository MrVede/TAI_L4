"use strict";
class Complex {
    Complex(let, x, let, im) {
        this.re = re;
        this.im = im;
    }
    add(Complex, c, Complex) {
        return new Complex(re + c.re, im + c.im);
    }
    ;
    difference(Complex, d) {
        return new Complex(re + d.re, im + d.im);
    }
    ;
    show() {
        console.log;
    }
    ;
    main(String, [], argv) {
    }
    ;
}
