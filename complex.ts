

class Complex {
    private re:string;
    private im:; 

    public Complex(let x, let im) {
        this.re = re;
        this.im = im;
    }

 public add(Complex c, Complex ) {
        return new Complex(re + c.re, im + c.im);
};

 public difference(Complex d) {
     return new Complex(re + d.re, im + d.im);
 };

 public show() {

     console.log
 };



public static void main(String[] argv){



};